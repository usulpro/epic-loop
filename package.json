{
  "name": "epic-loop",
  "version": "0.1.0",
  "private": true,
  "description": "Codex plugin for orchestrating long-lived epic-level software work across sessions.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-epic-loop-package.mjs && for f in plugins/epic-loop/skills/epic-loop/scripts/*.mjs plugins/epic-loop/skills/epic-loop/scripts/lib/*.mjs; do node --check \"$f\" || exit 1; done",
    "validate:epic-loop": "pnpm run validate",
    "doctor:epic-loop": "node plugins/epic-loop/skills/epic-loop/scripts/doctor.mjs --json"
  },
  "keywords": ["codex", "codex-plugin", "agent-skills", "epic", "automation", "hooks"],
  "packageManager": "pnpm@10.11.0"
}
